# Ember Bootbox Shim

Ember Shim for http://bootboxjs.com.

```sh

ember install ember-bootbox-shim

```

This lib needs bootstrap. You can install with:

```sh

ember install ember-bootstrap-shim

```
